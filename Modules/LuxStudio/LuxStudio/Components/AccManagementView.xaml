<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="LuxStudio.Components.AccManagementView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LuxStudio.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20">

        <!-- Loading Panel -->
        <StackPanel x:Name="LoadingPanel" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="10">
            <ProgressRing IsActive="True" Width="40" Height="40"/>
            <TextBlock Text="Initializing Authentication Service..." FontSize="16" HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Input Panel -->
        <StackPanel x:Name="InputPanel" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="10">
            <TextBlock Text="LuxStudio Address" FontSize="18" />
            <TextBox PlaceholderText="Enter address here..." Width="300"/>
            <Button Content="Connect" Width="100"/>
        </StackPanel>

    </Grid>
</Page>
