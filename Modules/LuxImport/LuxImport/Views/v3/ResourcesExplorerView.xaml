<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="LuxImport.Views.v3.ResourcesExplorerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LuxImport.Views.v3"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <TreeView x:Name="ExplorerTree"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  Expanding="ExplorerTree_Expanding"
                  SelectionMode="Single"
                  IsDoubleTapEnabled="True"
                  DoubleTapped="ExplorerTree_DoubleTapped"
                  Collapsed="ExplorerTree_Collapsed"
        >
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <Setter Property="HasUnrealizedChildren" Value="True" />
                </Style>
            </TreeView.ItemContainerStyle>
            <TreeView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{Binding BitmapImage}" Height="24" Width="24" />
                        <TextBlock Margin="0,0,10,0" />
                        <TextBlock Text="{Binding DisplayText}" VerticalAlignment="Center" Margin="0,0,10,0" />
                    </StackPanel>
                </DataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</Page>
