<Page
    x:Class="LuxImport.Views.ImportView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LuxImport"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <!-- Main Grid -->
    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Buttons -->
            <RowDefinition Height="Auto"/>
            <!-- Input & Actions -->
            <RowDefinition Height="*"/>
            <!-- File List -->
        </Grid.RowDefinitions>

        <!-- Browse Folder Button -->
        <Button Content="Browse Folder" Click="BrowseFolder_Click" HorizontalAlignment="Stretch" Padding="10,5"/>

        <!-- Separator -->
        <Border Grid.Row="1" Height="1" Background="Gray" Margin="10,10,10,10"/>

        <!-- Collection Name Input -->
        <StackPanel Grid.Row="2" Orientation="Vertical" Visibility="Collapsed" x:Name="CollectionInputPanel" Margin="10,0,10,0">
            <TextBox x:Name="CollectionNameInput" PlaceholderText="Enter Collection Name" Margin="0,10,0,0"/>
            <Button x:Name="CreateCollectionButton" Content="Create Collection" Margin="0,5,0,0"/>
        </StackPanel>


        <!-- Indexing Text -->
        <TextBlock x:Name="IndexingText" Text="Indexing in progress..." Visibility="Collapsed" Grid.Row="2" Margin="10,10,0,0"/>
    </Grid>
</Page>
