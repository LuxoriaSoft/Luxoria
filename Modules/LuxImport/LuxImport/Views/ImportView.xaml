<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="LuxImport.Views.ImportView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LuxImport"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- Header -->
            <RowDefinition Height="*" />
            <!-- Main Content -->
            <RowDefinition Height="Auto" />
            <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <StackPanel Orientation="Horizontal" Background="LightGray" Padding="10">
            <TextBlock Text="LuxImport - Photo Session Import" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Main Content Section -->
        <StackPanel Grid.Row="1" Padding="20">
            <TextBlock Text="Select Files to Import:" FontWeight="Bold" FontSize="18" Margin="0,0,0,10"/>
            <Button Content="Browse Files" Click="BrowseFiles_Click" Width="150"/>

            <ListView Name="FileListView" Margin="0,10">
                <ListView.Header>Selected Files</ListView.Header>
            </ListView>

            <ProgressBar Name="ImportProgress" Minimum="0" Maximum="100" Value="0" Height="20" Margin="0,10" Visibility="Collapsed"/>
            <StackPanel Orientation="Horizontal" Spacing="10">
                <Button Content="Start Import" Click="StartImport_Click" Width="150"/>
                <Button Content="Cancel" Click="CancelImport_Click" Width="150"/>
            </StackPanel>
        </StackPanel>

        <!-- Footer Section -->
        <StackPanel Grid.Row="2" Background="LightGray" Padding="10">
            <TextBlock Text="LuxImport - Manage Your Photo Sessions Efficiently" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>
