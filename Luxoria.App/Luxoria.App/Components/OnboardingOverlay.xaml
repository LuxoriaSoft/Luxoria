<UserControl
    x:Class="Luxoria.App.Components.OnboardingOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="600"
    d:DesignWidth="800">

    <Grid x:Name="RootGrid">
        <!-- Semi-transparent overlay -->
        <Grid x:Name="OverlayBackground" Background="#66000000" />

        <!-- Highlight border in a Popup (appears above flyouts) -->
        <Popup x:Name="HighlightPopup" IsOpen="False">
            <Border
                x:Name="HighlightBorder"
                BorderBrush="#FF00A2ED"
                BorderThickness="3"
                CornerRadius="8"
                Background="Transparent"
                Width="100"
                Height="40">
                <Border.Shadow>
                    <ThemeShadow />
                </Border.Shadow>
            </Border>
        </Popup>

        <!-- Tooltip container -->
        <Canvas x:Name="TooltipCanvas">
            <Border
                x:Name="TooltipBorder"
                Background="#FF2b2b2b"
                BorderBrush="#FF00A2ED"
                BorderThickness="1"
                CornerRadius="8"
                Padding="24"
                MaxWidth="400"
                Visibility="Collapsed">
                <Border.Shadow>
                    <ThemeShadow />
                </Border.Shadow>

                <StackPanel Spacing="12">
                    <!-- Progress indicator -->
                    <TextBlock
                        x:Name="ProgressText"
                        FontSize="11"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        HorizontalAlignment="Center"
                        Text="Step 1 of 3" />

                    <!-- Title -->
                    <TextBlock
                        x:Name="TitleText"
                        FontSize="18"
                        FontWeight="SemiBold"
                        TextWrapping="Wrap"
                        Text="Welcome to Luxoria!" />

                    <!-- Description -->
                    <TextBlock
                        x:Name="DescriptionText"
                        FontSize="13"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        TextWrapping="Wrap"
                        LineHeight="18"
                        Text="Let's get you started with installing some modules to enhance your experience." />

                    <!-- Navigation buttons -->
                    <Grid ColumnDefinitions="Auto,*,Auto,Auto" ColumnSpacing="8">
                        <Button
                            x:Name="BackButton"
                            Grid.Column="0"
                            Content="Back"
                            Click="BackButton_Click"
                            Visibility="Collapsed" />

                        <Button
                            x:Name="SkipButton"
                            Grid.Column="1"
                            Content="Skip Tour"
                            HorizontalAlignment="Left"
                            Click="SkipButton_Click" />

                        <Button
                            x:Name="NextButton"
                            Grid.Column="2"
                            Content="Next"
                            Background="{ThemeResource AccentButtonBackground}"
                            Foreground="{ThemeResource AccentButtonForeground}"
                            Click="NextButton_Click" />

                        <Button
                            x:Name="FinishButton"
                            Grid.Column="3"
                            Content="Got it!"
                            Background="{ThemeResource AccentButtonBackground}"
                            Foreground="{ThemeResource AccentButtonForeground}"
                            Click="FinishButton_Click"
                            Visibility="Collapsed" />
                    </Grid>
                </StackPanel>
            </Border>
        </Canvas>
    </Grid>
</UserControl>
