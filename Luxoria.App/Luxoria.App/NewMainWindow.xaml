<Window
    x:Class="Luxoria.App.NewMainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Luxoria.App"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:components="using:Luxoria.App.Components"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    Title="Luxoria App."
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- Main Menu Bar -->
            <RowDefinition Height="*" />
            <!-- Content Area -->
            <RowDefinition Height="5" />
            <!-- Splitter -->
            <RowDefinition Height="150" MinHeight="100" MaxHeight="300" />
            <!-- Bottom Panel -->
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" MinWidth="150" MaxWidth="500" x:Name="LeftPanelColumn"/>
            <!-- Left Panel -->
            <ColumnDefinition Width="Auto" />
            <!-- Splitter -->
            <ColumnDefinition Width="*" />
            <!-- Center Area -->
            <ColumnDefinition Width="Auto" />
            <!-- Splitter -->
            <ColumnDefinition Width="200" MinWidth="150" MaxWidth="500" x:Name="RightPanelColumn"/>
            <!-- Right Panel -->
        </Grid.ColumnDefinitions>

        <!-- Main Menu Bar -->
        <components:MainMenuBarComponent Grid.Row="0" Grid.ColumnSpan="5" />

        <!-- Left Panel -->
        <Border Grid.Row="1" Grid.Column="0" Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" Margin="10, 10, 0, 10" CornerRadius="8, 0, 0, 8">
            <TextBlock Text="Left Panel" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}" />
        </Border>

        <!-- Vertical Splitter Between Left and Center -->
        <controls:GridSplitter 
            Grid.Row="1" 
            Grid.Column="1" 
            Width="1" 
            HorizontalAlignment="Stretch" 
            VerticalAlignment="Stretch" 
            ResizeBehavior="PreviousAndNext" 
            Background="{ThemeResource ControlStrongFillColorDisabledBrush}" 
            Margin="0, 10, 0, 10" 
            CornerRadius="0, 8, 8, 0"
            />

        <!-- Center Panel -->
        <Border Grid.Row="1" Grid.Column="2" Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" Margin="10" CornerRadius="8">
            <TextBlock Text="Center Area" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}" />
        </Border>

        <!-- Vertical Splitter Between Center and Right -->
        <controls:GridSplitter 
            Grid.Row="1" 
            Grid.Column="3" 
            Width="1" 
            HorizontalAlignment="Stretch" 
            VerticalAlignment="Stretch" 
            ResizeBehavior="PreviousAndNext" 
            Background="{ThemeResource ControlStrongFillColorDisabledBrush}" 
            Margin="0, 10, 0, 10" 
            CornerRadius="8, 0, 0, 8" />

        <!-- Right Panel -->
        <Border Grid.Row="1" Grid.Column="4" Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" Margin="0, 10, 10, 10" CornerRadius="0, 8, 8, 0">
            <TextBlock Text="Right Panel" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}" />
        </Border>

        <!-- Horizontal Splitter Between Content Area and Bottom Panel -->
        <controls:GridSplitter 
            Grid.Row="2" 
            Grid.Column="0" 
            Grid.ColumnSpan="5" 
            Height="1" 
            HorizontalAlignment="Stretch" 
            VerticalAlignment="Stretch" 
            ResizeBehavior="PreviousAndNext" 
            Background="{ThemeResource ControlStrongFillColorDisabledBrush}"
            Margin="10, 0, 10, 0" 
            CornerRadius="8, 8, 0 ,0" />

        <!-- Bottom Panel -->
        <Border Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="5" Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" Margin="10, 0, 10, 10" CornerRadius="0, 0, 8, 8">
            <TextBlock Text="Bottom Panel" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}" />
        </Border>

    </Grid>
</Window>